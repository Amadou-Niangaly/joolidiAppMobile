import 'package:flutter/material.dart';

class DemandFormPage extends StatelessWidget {
  const DemandFormPage({super.key});

  @override
  Widget build(BuildContext context) {
     return Scaffold(
      appBar: AppBar(
        title: const Text("les demandes"),
      ),
      
    );
  }
}