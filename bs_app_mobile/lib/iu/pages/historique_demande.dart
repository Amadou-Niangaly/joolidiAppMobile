import 'package:flutter/material.dart';

class HistoriqueDemandePage extends StatelessWidget {
  const HistoriqueDemandePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(

        ),
        body: Center(
          child: Text("pas d'historique pour le moment"),
        ),
    );
  }
}