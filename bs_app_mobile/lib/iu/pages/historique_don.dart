import 'package:flutter/material.dart';

class HistoriqueDonPage extends StatelessWidget {
  const HistoriqueDonPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(

        ),
        body: Center(
          child: Text("pas d'historique pour le moment"),
        ),
    );
  }
}